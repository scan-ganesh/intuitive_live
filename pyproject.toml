[project]
name = "intuitive-live"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "breeze-connect>=1.0.69",
    "dotenv>=0.9.9",
    "neo-api-client",
    "python-dotenv>=1.0.0",
]

[tool.uv.sources]
neo-api-client = { git = "https://github.com/Kotak-Neo/Kotak-neo-api-v2.git", rev = "v2.0.1" }
